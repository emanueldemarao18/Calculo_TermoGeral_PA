#include<stdio.h>
#include<math.h>
	main(){
		// Programa do Emanuel para calcular o termo Geral
		int x,contador=0,f=0,an,a1,n=0,r;
		printf("Digite (a1):");
		scanf("%d",&a1);
		printf("Digite (an):");
			scanf("%d",&an);
			printf("Digite (r):");
			scanf("%d",&r);
			n=((an-a1)/r)+1;
	printf("%d",n);
}

